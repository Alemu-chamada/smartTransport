{
  "name": "smart-transport-root",
  "private": true,
  "description": "Monorepo root - backend in /backend, frontend in /frontend",
  "scripts": {
    "start": "cd backend && node src/server.js",
    "build": "cd backend && npm install"
  }
}
